# Frontend

Next.js client for PromptArena. See the repository root README for setup.

**Windows:**

```powershell
.\start.ps1
```

**Manual:**

```powershell
npm install
copy .env.local.example .env.local
$env:NODE_ENV = "development"
npm run dev
```
